# libs/

This folder is no longer needed. Mods are now pure JSON — no DLLs or compilation required.

This folder is kept for backward compatibility with older SDK versions.
See the root README.md for the current modding guide.
